# Django Core
Django==4.2.7
djangorestframework==3.14.0
djangorestframework-simplejwt==5.3.0
django-cors-headers==4.3.1
django-filter==23.5
channels==4.0.0
whitenoise==6.6.0

# Database (SQL Server)
mssql-django==1.4
# Use a pyodbc wheel that supports Python 3.13 on Windows
pyodbc==5.2.0

# Redis / Celery
redis==5.0.1
celery==5.3.4
django-redis==5.4.0

# API and Schema
drf-spectacular==0.26.5

# Document and File Handling
python-docx==1.1.0
docxtpl==0.16.7
Pillow==11.3.0
python-magic==0.4.27

# External AI APIs
openai==1.3.7
anthropic==0.7.8
google-generativeai==0.3.2

# Utilities
requests==2.31.0
python-dotenv==1.0.0
django-environ==0.11.2
## Use a cryptography build compatible with modern Python versions/wheels
cryptography==46.0.2
python-dateutil==2.8.2
pytz==2023.3

# --- HEAVY LOCAL AI MODELS ---
# The following packages are for running AI models directly on your server.
# They require significant RAM (8GB+) and CPU resources.
# If your production server is not powerful enough, this can cause crashes or extreme slowness.
# Consider commenting them out if you only plan to use external AI APIs for the initial launch.
# transformers==4.36.2
# torch==2.1.2
# torchvision==0.16.2
# sentence-transformers==2.2.2
# gpt4all==2.0.2
